import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import 'normalize.css'; // normalize css to reset the browser css, while we are working with bootstrap do we need to reset the browser's css
import './index.css';

ReactDOM.render(<App />, document.getElementById('root'));

